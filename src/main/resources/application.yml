spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/test_users_db
    username: root
    password: 0000
    driver-class-name: org.mariadb.jdbc.Driver

mybatis:
  mapper-locations: classpath:resources/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true