<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beyond.beatbuddy.user.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.beyond.beatbuddy.user.entity.User">
        <id property="userId" column="user_id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="gender" column="gender"/>
        <result property="birthYear" column="birth_year"/>
        <result property="profileImageUrl" column="profile_image_url"/>
        <result property="status" column="status"/>
    </resultMap>

    <select id="selectUserByEmail" resultMap="userResultMap">
        SELECT user_id, email, password, nickname, gender,
               birth_year, profile_image_url, status
        FROM users
        WHERE email = #{email}
          AND status = 'ACTIVE'
    </select>

    <update id="updatePassword">
        UPDATE users
        SET password = #{newPassword},
            updated_at = NOW()
        WHERE user_id = #{userId}
    </update>

</mapper>